const DICT_APIS = [
    // 


]

/**
 * 1、分两类：
 *    a. 传key
 *    b. 调接口
 * 2、封装：
 *    字典的key传进来，都要先遍历一遍，搜索到具体配置
 *       a. key type api
 *          字典时，没有特殊参数
 *          业务数据时，有特殊参数
 *       b. 一个遍历方法
 *       c. 一个具体执行查询的方法
 *       d. 可能要有一个返回值转格式的方法 
 *       e. 
 * 
 * 
 */