/**
 * 以下为 filter 定义示例
 * nullValue，即是过滤器名
 * mars-ui-frame 中已定义一些过滤器，有需要的情况，仍可以自定义过滤器，示例如下：
 * 要求：每个过滤器，都是单独 export
 * @param value 参数
 */
// export function nullValue(value) {
//     if (value === 0) {
//         return 0
//     } else {
//         if (!value) return '--'
//         else return value
//     }
// }
