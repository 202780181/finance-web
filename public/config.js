let JCPT_GATEWAY_URL = 'http://192.168.50.100:8983'
let JCPT_HREF_URL = 'http://192.168.50.100:8983'
var WIN_GLOBAL = {
    "REQ_URL": {
        "JCPT_HREF_URL": JCPT_HREF_URL,  // 页面前端地址
        "JCPT_GATEWAY_URL": JCPT_GATEWAY_URL,  // 页面后端地址
    },
    "THEME_SET": "in",  // 使用 navbar 上的主题设置，还是用 右上角菜单内的 主题设置
    "SYSNAME": {    // 可以不配置，配置了则按此显示，该配置的优先级更高
        "CHINESE_NAME": "会计核算系统",
        "ENGLISH_NAME": "ACCOUNTING SYSTEM",
    },

    "LOGO_URL": 'logo.png',
}
